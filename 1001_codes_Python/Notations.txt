-'A'             : an uppercase for a letter means a matrix

-'û'             : a ^ over a letter means a vector

-'A*'            : a * beside a Matrix means transpose of that Matrix

-'n,p,q'         : are integers

-'&'             : means belongs

-i,j,k           : are indices, having ai,aj,ak means that a is indiced by i,j or k, is available too
for k+1,i+1,j+1,k-1,i-1,j-1.
-sum(exp(i),p,n) : sigma of exp(i) from p to n

-||(exp(i),p,n)  : produc of exp(i) from p to n

